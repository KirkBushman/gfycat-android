<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kirkbushman.sampleapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GfycatApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.VisualizeTokenActivity" />
        <activity android:name=".activities.GetGfycatActivity" />
        <activity android:name=".activities.SelfActivity" />
        <activity android:name=".activities.GetUserActivity" />
        <activity android:name=".activities.GetUserFeedActivity" />
        <activity android:name=".activities.GfycatActivity" />
        <activity android:name=".activities.StickersActivity" />
        <activity android:name=".activities.SearchStickersActivity" />
        <activity android:name=".activities.TrendingGfycatActivity" />
        <activity android:name=".activities.TrendingTagsActivity" />

    </application>
</manifest>